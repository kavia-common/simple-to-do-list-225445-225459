# todo_frontend

Local-only React app (Vite) for a simple to-do list with optional reminders.

## Run
```bash
npm install
npm run dev
```

## Notes
- Data persists in `localStorage`.
- Reminders are scheduled locally and will play a short beep when due.
- The reminder alert UI is implemented as an accessible modal with dismiss and snooze actions.
